% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{count_detections}
\alias{count_detections}
\title{Count detections per species across image sequences}
\usage{
count_detections(
  detections,
  station_col = "station",
  confidence_threshold = 0.5,
  iou_threshold = 0.9,
  maxdiff = 60,
  max_n = NULL,
  classes = NULL
)
}
\arguments{
\item{detections}{dataframe, output of parse_detections()}

\item{station_col}{column name representing the station or camera,
default = "station"}

\item{confidence_threshold}{minimum confidence to consider a
detection, default = 0.5}

\item{iou_threshold}{IOU threshold for duplicate detection removal
(see deduplicate()), default = 0.9}

\item{maxdiff}{max time difference in seconds between images in a
sequence, default = 60}

\item{max_n}{optional max number of images per sequence to
consider, default = NULL (no limit)}

\item{classes}{character vector of category_label values to count.
NULL (default) counts every class found, except "empty".}
}
\value{
dataframe, one row per sequence with averaged counts per species
}
\description{
Groups detections into sequences per station (via the existing
sequence_calculation()), then counts detections per species,
averaged across the images within each sequence.
}
\examples{
\dontrun{
counts <- count_detections(detections, station_col = "station")
}
}
